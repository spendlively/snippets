two step view - двухэтапное представление

Определение: выполняет визуализацию данных домена в два этапа,
сначала в некое подобие логической страницы,
затем в код HTML

Назначение: 
- веб приложения с несколькими вариантами внешнего вида
- веб приложения с одним вариантом внешнего вида (оформление всех страниц в одном стиле)