
//1. Индексация строки ссылается на БАЙТ!
str := "абв"
fmt.Printf("%T %v %q\n", str[0], str[0], str[0]) //uint8 208 'Ð'

//2. В то время как итерация по строке ссылкается на РУНУ!
for _, v := range "абв" {
	fmt.Printf("%T %v %q\n", v, v, v) //int32 1072 'а'
}