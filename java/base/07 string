Любая символьная строка является объектом класса String.

Объекты неизменяемые

Создание
String myString = "Это тестовая строка";

Конкатенируются с помощью операции +

Методы
equials()
charAt()
length()

Аргументы переменной длины
Аргумент a неявно объявляется как массив типа int
int test(int ... a){
  for(int x: a){}
}

Параметр переменной длины должен быть последним

Методы с переменным количеством аргументов можно перегружать

При перегрузке методов с аргументамип переменной длины может быть ошибка при компиляции из-за неоднозначности - например если вызвать метод без параметров.

