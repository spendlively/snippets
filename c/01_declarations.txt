
// Целое
int a;

// Указатель на целое
int *b;

// Указатель на указатель на целое
int **c;

// Массив из десяти целых
int d[10];

// Массив из десяти указателей на целые
int *e[10];

// Указатель на массив из десяти целых
int (*f)[10];

// Указатель на функцию, которая берет целый аргумент и возвращает целое
int (*g)(int);

// Массив из десяти указателей на функции, которые берут целый аргумент и возвращают
int (*h[10])(int);
