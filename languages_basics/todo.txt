Модульность: иерархия (класс/функция, файл, пакет/неймспейс/модуль, библиотека)

Импорт: синтаксис, что есть что при импорте, откуда импортируется код, импорт исходников, импорт скомпиленого кода

Менеджер пакетов: основные команды (обновить кэш, просмотреть список установленных пакетов, найти пакет для уснановки, установить, обновить, удалить), откуда скачивает, куда скачивает

Распространение: исходники, билды, deb- пакеты

Обработка ошибок: эксепшны (выкинуть, поймать), ошибки (выкинуть, поймать)

Логирование: инициализация, выбор файла для вывода, выбор уровня, запись

Отладка: локальная, удаленная

Принцип работы: кто управляет процессом,   сколько потоков, байткод
